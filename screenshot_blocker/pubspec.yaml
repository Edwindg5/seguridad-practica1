# pubspec.yaml
name: loginoutscreen
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  # Librerías para detectar ubicación y fake GPS
  location: ^7.0.0
  permission_handler: ^11.3.1

  cupertino_icons: ^1.0.8

  # 🔥 Firebase - AGREGAR ESTO:
  firebase_core: ^3.0.0
  firebase_messaging: ^15.0.0
  flutter_local_notifications: ^17.0.0
  flutter_secure_storage: ^9.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true